/*!
 * Copyright (C) Microsoft Corporation. All rights reserved.
 */

import {
  ComponentPage,
  ExampleCard,
  IComponentDemoPageProps,
  PropertiesTableSet
} from '@fluentui/react-docsite-components';
import * as React from 'react';
import { LoadingButtonExample } from './examples/LoadingButton.Example';

import LoadingButtonExampleCode from '!!raw-loader!../../../src/components/LoadingButton/examples/LoadingButton.Example.tsx';
import LoadingButtonPropertiesTableSet from '!!raw-loader!../../../src/components/LoadingButton/LoadingButton.types.ts';

export class LoadingButtonPage extends React.Component<IComponentDemoPageProps, {}> {
  public render(): JSX.Element {
    return (
      <ComponentPage
        styles={{ navigation: { display: 'none' } }}
        title={'Loading Button'}
        componentName="LoadingButton"
        exampleCards={
          <div>
            <ExampleCard title="Loading Button" code={LoadingButtonExampleCode}>
              <LoadingButtonExample />
            </ExampleCard>
          </div>
        }
        propertiesTables={<PropertiesTableSet sources={[LoadingButtonPropertiesTableSet]} />}
        overview={
          <div>
            <p>
              The Loading Button is a Higher-Order Component that takes in an IButton and gives it the ability
              to show a Spinner in place of the icon. It is provided as the Higher-Order Component function
              wrapper itself, as well as pre-wrapped versions of the Action, Command, Default, and Primary
              buttons from Offfice Fabric.
            </p>
          </div>
        }
        isHeaderVisible={this.props.isHeaderVisible}
      />
    );
  }
}
