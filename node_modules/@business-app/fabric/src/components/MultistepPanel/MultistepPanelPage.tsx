/*!
 * Copyright (C) Microsoft Corporation. All rights reserved.
 */

import {
  ComponentPage,
  ExampleCard,
  IComponentDemoPageProps,
  PropertiesTableSet
} from '@fluentui/react-docsite-components';
import * as React from 'react';
import { MultistepPanelBasicExample } from './examples/MultistepPanel.Basic.Example';

import PanelBasicExampleCode from '!!raw-loader!../../../src/components/MultistepPanel/examples/MultistepPanel.Basic.Example.tsx';
import MultistepPanelPropertiesTableSet from '!!raw-loader!../../../src/components/MultistepPanel/MultistepPanel.types.tsx';

export class MultistepPanelPage extends React.Component<IComponentDemoPageProps, {}> {
  public render(): JSX.Element {
    return (
      <ComponentPage
        styles={{ navigation: { display: 'none' } }}
        title={'Panel'}
        componentName="PanelExample"
        exampleCards={
          <div>
            <ExampleCard title="Basic panel" code={PanelBasicExampleCode}>
              <MultistepPanelBasicExample />
            </ExampleCard>
          </div>
        }
        propertiesTables={<PropertiesTableSet sources={[MultistepPanelPropertiesTableSet]} />}
        overview={
          <div>
            The MultistepPanel wraps the Business App Fabric Panel, providing easy navigation between multiple
            steps and differentiating between props that affect the MultistepPanel as a whole (e.g. is it
            dismissable?) versus props that affect individual panel steps (i.e. the content).
          </div>
        }
        isHeaderVisible={this.props.isHeaderVisible}
      />
    );
  }
}
