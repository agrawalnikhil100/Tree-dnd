/*!
 * Copyright (C) Microsoft Corporation. All rights reserved.
 */

import * as React from 'react';
import { calculateWordHighlights } from '../Helpers';
import { HighlightedText } from '../HighlightedText';

export class HighlightedTextCharactersExample extends React.Component {
  public render(): JSX.Element {
    const text = "Multi-byte characters are supported 🙃 - and so is RTL: it's \u200Fمغامرة جديدة";
    const highlights = calculateWordHighlights(text, ['🙃', 'جديدة', 'RTL']);
    return (
      <div>
        <HighlightedText
          text={text}
          highlights={highlights}
          labels={{
            beginHighlightedText: 'Begin highlighted text',
            endHighlightedText: 'End highlighted text'
          }}
        />
      </div>
    );
  }
}
