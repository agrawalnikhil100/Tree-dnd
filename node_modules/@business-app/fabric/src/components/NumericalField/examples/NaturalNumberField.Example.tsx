/*!
 * Copyright (C) Microsoft Corporation. All rights reserved.
 */

import * as React from 'react';
import { NaturalNumberField } from '../';

export interface INaturalNumberFieldExampleState {
  readonly currentValue: number | undefined;
}

export class NaturalNumberFieldExample extends React.Component<{}, INaturalNumberFieldExampleState> {
  constructor(props: {}) {
    super(props);

    this.state = {
      currentValue: 0
    };
  }

  public render(): JSX.Element {
    return (
      <div style={{ display: 'flex', flexDirection: 'column' }}>
        <NaturalNumberField
          textFieldProps={{
            label: 'Natural number example'
          }}
          value={this.state.currentValue}
          onBlurred={this.onChanged}
        />
        <p>Last valid value: {this.state.currentValue}</p>
      </div>
    );
  }

  private onChanged = (newValue: number | undefined) => {
    this.setState({
      currentValue: newValue
    });
  };
}
