/*!
 * Copyright (C) Microsoft Corporation. All rights reserved.
 */
import {
  ComponentPage,
  ExampleCard,
  IComponentDemoPageProps,
  PropertiesTableSet
} from '@fluentui/react-docsite-components';
import * as React from 'react';

import { ResizerBasicExample } from './examples/Resizer.Basic.Example';

import ResizerExampleCode from '!!raw-loader!../../../src/components/Resizer/examples/Resizer.Basic.Example.tsx';
import ResizerPropertiesTableSet from '!!raw-loader!../../../src/components/Resizer/Resizer.types.ts';

export function ResizerPage(props: IComponentDemoPageProps) {
  return (
    <ComponentPage
      styles={{ navigation: { display: 'none' } }}
      title={'Resizer'}
      componentName={'Resizer'}
      exampleCards={
        <div>
          <ExampleCard title="Button with a Resizer at the right edge" code={ResizerExampleCode}>
            <ResizerBasicExample />
          </ExampleCard>
        </div>
      }
      propertiesTables={<PropertiesTableSet sources={[ResizerPropertiesTableSet]} />}
      overview={
        <div>
          <p>
            The Resizer component can be used to resize any element by dragging the resizer. You can attach
            the Resizer to any of the edges of the element you want to resize(top, right, bottom or left).
            Note: The Resizer component requires its parent element to be positioned. To see the resizer,
            hover over the edge on which it is positioned.
          </p>
        </div>
      }
      isHeaderVisible={props.isHeaderVisible}
    />
  );
}
