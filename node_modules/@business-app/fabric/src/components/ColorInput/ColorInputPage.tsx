/*!
 * Copyright (C) Microsoft Corporation. All rights reserved.
 */

import ColorInputxampleCode from '!!raw-loader!../../../src/components/ColorInput/examples/ColorInput.Basic.Example.tsx';
import ColorInputPropertiesSet from '!!raw-loader!../../../src/components/ColorInput/ColorInput.types.ts';
import * as React from 'react';

import { ComponentPage, ExampleCard, PropertiesTableSet } from '@fluentui/react-docsite-components';

import { ColorInputBasicExample } from './examples/ColorInput.Basic.Example';

export class ColorInputPage extends React.Component<{}, {}> {
  public render(): JSX.Element {
    return (
      <ComponentPage
        styles={{ navigation: { display: 'none' } }}
        title={'ColorInput'}
        componentName="ColorInput"
        exampleCards={
          <div>
            <ExampleCard title="ColorInput" code={ColorInputxampleCode}>
              <ColorInputBasicExample />
            </ExampleCard>
          </div>
        }
        propertiesTables={<PropertiesTableSet sources={[ColorInputPropertiesSet]} />}
        overview={
          <div>
            <p>
              The color input component (ColorInput) is used to browse and select colors. It lets consumers
              browse through colors on the color spectrum; specify a color in either in RGB (Red-Green-Blue);
              alpha color code; or Hexadecimal textboxes.
            </p>
          </div>
        }
        isHeaderVisible={true}
      />
    );
  }
}
