/*!
 * Copyright (C) Microsoft Corporation. All rights reserved.
 */

import { IStyleFunction } from '@fluentui/react/lib/Utilities';
import { IFormDatePickerStyleProps, IFormDatePickerStyles } from './FormDatePicker.types';

export const getStyles: IStyleFunction<IFormDatePickerStyleProps, IFormDatePickerStyles> = ({
  theme: { palette, fonts }
}) => ({
  root: 'form-date-picker',
  inputError: [
    'input-error',
    fonts.small,
    {
      color: palette.redDark,
      display: 'flex',
      flexDirection: 'row',
      alignItems: 'center',
      selectors: {
        '> i': {
          // @include ms-margin-right(5px),
          marginTop: 'auto',
          fontSize: 14 // Fabric TextField's validation icon size
        }
      }
    }
  ]
});
