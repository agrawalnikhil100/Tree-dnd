/*!
 * Copyright (C) Microsoft Corporation. All rights reserved.
 */

import { IStyleFunction } from '@fluentui/react/lib/Utilities';
import { PresenceStyleProps, PresenceStyles } from './Presence.types';

export const getPresenceStyles: IStyleFunction<PresenceStyleProps, PresenceStyles> = ({ showAccordion }) => ({
  commandBarPersonaButton: {
    padding: 0,
    border: 'none',
    height: 24,
    minWidth: 24,
    '&:focus': {
      '::after': {
        left: '-1px !important',
        top: '-1px !important',
        bottom: '-1px !important',
        right: '-1px !important'
      }
    }
  },
  overFlowListContent: {
    height: '100%',
    maxHeight: showAccordion ? 380 : 112,
    overflowY: 'auto',
    overflowX: 'hidden'
  }
});
