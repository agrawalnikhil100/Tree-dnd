/*!
 * Copyright (C) Microsoft Corporation. All rights reserved.
 */

import { Label } from '@fluentui/react/lib/Label';
import * as React from 'react';
import { itemClassName } from './SettingsExplorer.styles';
import { ISettingsItemProps } from './SettingsExplorer.types';

export const SettingsItem: React.FunctionComponent<ISettingsItemProps> = props => {
  return (
    <div className={itemClassName(props.itemStyles)}>
      <Label styles={{ root: { fontWeight: '600', fontSize: 16 } }}>{props.name}</Label>
      {props.children}
    </div>
  );
};
