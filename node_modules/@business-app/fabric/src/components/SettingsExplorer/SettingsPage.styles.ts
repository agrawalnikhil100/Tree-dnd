/*!
 * Copyright (C) Microsoft Corporation. All rights reserved.
 */

import { ISettingsPageStyleProps, ISettingsPageStyles } from './SettingsPage.types';

export function getStyles(props: ISettingsPageStyleProps): ISettingsPageStyles {
  const { theme } = props;

  return {
    root: {
      flexGrow: 1
    },
    body: {
      padding: 16,
      flexGrow: 1,
      display: 'flex',
      flexDirection: 'column',
      flexWrap: 'nowrap',
      overflow: 'auto'
    },
    header: {
      display: 'flex',
      flexFlow: 'row wrap',
      justifyContent: 'space-between'
    },
    headerName: {
      flexGrow: 1,
      fontSize: 18,
      fontWeight: 600,
      alignContent: 'flex-start',
      paddingLeft: 8
    },
    contentContainer: {
      flexGrow: 1,
      padding: 8,
      overflow: 'auto',
      marginBottom: 16,
      borderBottom: `1px solid ${theme?.palette?.neutralLight}`
    },
    footerButtons: {
      alignSelf: 'flex-end',
      paddingRight: 8
    }
  };
}
