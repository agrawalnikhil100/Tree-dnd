"use strict";
/*!
 * Copyright (C) Microsoft Corporation. All rights reserved.
 */
Object.defineProperty(exports, "__esModule", { value: true });
require("jest");
var Enzyme = require("enzyme");
var React = require("react");
var Button_1 = require("@fluentui/react/lib/Button");
var Link_1 = require("@fluentui/react/lib/Link");
var _1 = require("./");
describe('CardFooterBase', function () {
    it('Basic rendering of the Card Footer', function () {
        var wrapper = Enzyme.mount(React.createElement(_1.CardFooter, null));
        // should just render, w/o issue and no content
        var findPrimaryButton = wrapper.find(Button_1.PrimaryButton);
        expect(findPrimaryButton.length).toBe(0);
        var findLink = wrapper.find(Link_1.Link);
        expect(findLink.length).toBe(0);
        wrapper.unmount();
    });
    it('Render with Link and PrimaryButton', function () {
        var wrapper = Enzyme.mount(React.createElement(_1.CardFooter, { link: { text: 'Hyperlink' }, buttons: { primaryButton: { text: 'Button' } } }));
        var findPrimaryButton = wrapper.find(Button_1.PrimaryButton);
        expect(findPrimaryButton.length).toBe(1);
        var findLink = wrapper.find(Link_1.Link);
        expect(findLink.length).toBe(1);
        wrapper.unmount();
    });
});
//# sourceMappingURL=CardFooter.test.js.map