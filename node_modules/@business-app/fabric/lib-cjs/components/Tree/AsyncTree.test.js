"use strict";
/*!
 * Copyright (C) Microsoft Corporation. All rights reserved.
 */
Object.defineProperty(exports, "__esModule", { value: true });
var tslib_1 = require("tslib");
var enzyme_1 = require("enzyme");
var Icon_1 = require("@fluentui/react/lib/Icon");
var Link_1 = require("@fluentui/react/lib/Link");
var Spinner_1 = require("@fluentui/react/lib/Spinner");
var Styling_1 = require("@fluentui/react/lib/Styling");
var React = require("react");
var AsyncTree_1 = require("./AsyncTree");
var ExpandIcon_1 = require("./ExpandIcon/ExpandIcon");
var TreeNode_1 = require("./TreeNode/TreeNode");
var waitTime = 500;
var wait = function (ms) { return new Promise(function (r, j) { return setTimeout(r, ms); }); };
var onResolveItems = function (item) { return tslib_1.__awaiter(void 0, void 0, void 0, function () {
    var items, i, i;
    return tslib_1.__generator(this, function (_a) {
        switch (_a.label) {
            case 0: return [4 /*yield*/, wait(waitTime)];
            case 1:
                _a.sent();
                items = [];
                if (item === null) {
                    for (i = 0; i < 10; i++) {
                        items.push({
                            key: i.toString(),
                            label: "Label-" + i.toString()
                        });
                    }
                }
                else {
                    for (i = 0; i < 10; i++) {
                        items.push({
                            key: item.key + '/' + i.toString(),
                            label: item.label + '/' + i.toString(),
                            isFolder: i % 2 === 0
                        });
                    }
                }
                return [2 /*return*/, [items, null]];
        }
    });
}); };
var onResolveItemsWithFailures = function () {
    var failedItems = [];
    return (function (item) { return tslib_1.__awaiter(void 0, void 0, void 0, function () {
        var items, i, i;
        return tslib_1.__generator(this, function (_a) {
            switch (_a.label) {
                case 0: return [4 /*yield*/, wait(waitTime)];
                case 1:
                    _a.sent();
                    items = [];
                    if (item === null) {
                        for (i = 0; i < 10; i++) {
                            items.push({
                                key: i.toString(),
                                label: "Label-" + i.toString()
                            });
                        }
                    }
                    else if (failedItems.indexOf(item) >= 0) {
                        // Resolve items after failing initially.
                        for (i = 0; i < 10; i++) {
                            items.push({
                                key: item.key + '/' + i.toString(),
                                label: item.label + '/' + i.toString(),
                                isFolder: i % 2 === 0
                            });
                        }
                    }
                    else {
                        failedItems.push(item);
                        throw new Error();
                    }
                    return [2 /*return*/, [items, null]];
            }
        });
    }); });
};
var defaultStrings = {
    listAriaLabel: 'list aria label',
    moreCommandsAriaLabel: 'more commands aria label'
};
describe('AsyncTree', function () {
    beforeAll(function () {
        Styling_1.setIconOptions({ disableWarnings: true });
    });
    it('dynamically loads all the "root" items when mounted', function () { return tslib_1.__awaiter(void 0, void 0, void 0, function () {
        var wrapper;
        return tslib_1.__generator(this, function (_a) {
            switch (_a.label) {
                case 0:
                    wrapper = enzyme_1.mount(React.createElement(AsyncTree_1.AsyncTree, { onResolveItems: onResolveItems, strings: defaultStrings }));
                    // Wait for initial list of items to load.
                    return [4 /*yield*/, wait(waitTime + waitTime / 2)];
                case 1:
                    // Wait for initial list of items to load.
                    _a.sent();
                    wrapper.update();
                    expect(wrapper.find(TreeNode_1.TreeNode).length).toBe(10);
                    wrapper.unmount();
                    return [2 /*return*/];
            }
        });
    }); });
    it('correctly renders dynamically loaded items', function () { return tslib_1.__awaiter(void 0, void 0, void 0, function () {
        var wrapper;
        return tslib_1.__generator(this, function (_a) {
            switch (_a.label) {
                case 0:
                    wrapper = enzyme_1.mount(React.createElement(AsyncTree_1.AsyncTree, { onResolveItems: onResolveItems, strings: defaultStrings }));
                    // Wait for initial list of items to load.
                    return [4 /*yield*/, wait(waitTime + waitTime / 2)];
                case 1:
                    // Wait for initial list of items to load.
                    _a.sent();
                    wrapper.update();
                    wrapper.find(TreeNode_1.TreeNode).forEach(function (c, index) {
                        expect(c.text()).toBe('Label-' + index);
                    });
                    wrapper.unmount();
                    return [2 /*return*/];
            }
        });
    }); });
    it('shows a "Loading" sub-item with a spinner while dynamically expanding an item', function () { return tslib_1.__awaiter(void 0, void 0, void 0, function () {
        var wrapper, firstNode, loadingSubItem;
        return tslib_1.__generator(this, function (_a) {
            switch (_a.label) {
                case 0:
                    wrapper = enzyme_1.mount(React.createElement(AsyncTree_1.AsyncTree, { onResolveItems: onResolveItems, strings: defaultStrings }));
                    // Wait for initial list of items to load.
                    return [4 /*yield*/, wait(waitTime + waitTime / 2)];
                case 1:
                    // Wait for initial list of items to load.
                    _a.sent();
                    wrapper.update();
                    firstNode = wrapper.find(TreeNode_1.TreeNode).at(0);
                    // Expand the first node
                    firstNode.find(ExpandIcon_1.ExpandIcon).simulate('click');
                    expect(wrapper.find(TreeNode_1.TreeNode).length).toBe(11);
                    loadingSubItem = wrapper.find(TreeNode_1.TreeNode).at(1);
                    expect(loadingSubItem.find(Spinner_1.Spinner).length).toBe(1);
                    expect(loadingSubItem.text()).toBe('Loading...');
                    wrapper.unmount();
                    return [2 /*return*/];
            }
        });
    }); });
    it('hides the "Loading" sub-item after dynamically expanding an item', function () { return tslib_1.__awaiter(void 0, void 0, void 0, function () {
        var wrapper, firstNode;
        return tslib_1.__generator(this, function (_a) {
            switch (_a.label) {
                case 0:
                    wrapper = enzyme_1.mount(React.createElement(AsyncTree_1.AsyncTree, { onResolveItems: onResolveItems, strings: defaultStrings }));
                    // Wait for initial list of items to load.
                    return [4 /*yield*/, wait(waitTime + waitTime / 2)];
                case 1:
                    // Wait for initial list of items to load.
                    _a.sent();
                    wrapper.update();
                    firstNode = wrapper.find(TreeNode_1.TreeNode).at(0);
                    // Expand the first node
                    firstNode.find(ExpandIcon_1.ExpandIcon).simulate('click');
                    expect(wrapper.find(TreeNode_1.TreeNode).length).toBe(11);
                    expect(wrapper.find(TreeNode_1.TreeNode).find(Spinner_1.Spinner).length).toBe(1);
                    // Wait for list of items to load.
                    return [4 /*yield*/, wait(waitTime + waitTime / 2)];
                case 2:
                    // Wait for list of items to load.
                    _a.sent();
                    wrapper.update();
                    expect(wrapper.find(TreeNode_1.TreeNode).length).toBe(20);
                    expect(wrapper.find(TreeNode_1.TreeNode).find(Spinner_1.Spinner).length).toBe(0);
                    wrapper.unmount();
                    return [2 /*return*/];
            }
        });
    }); });
    it('shows multiple "Loading" sub-items spinning concurrently while dynamically expanding multiple items', function () { return tslib_1.__awaiter(void 0, void 0, void 0, function () {
        var wrapper, _loop_1, i;
        return tslib_1.__generator(this, function (_a) {
            switch (_a.label) {
                case 0:
                    wrapper = enzyme_1.mount(React.createElement(AsyncTree_1.AsyncTree, { onResolveItems: onResolveItems, strings: defaultStrings }));
                    // Wait for initial list of items to load.
                    return [4 /*yield*/, wait(waitTime + waitTime / 2)];
                case 1:
                    // Wait for initial list of items to load.
                    _a.sent();
                    wrapper.update();
                    expect(wrapper.find(TreeNode_1.TreeNode).find(Spinner_1.Spinner).length).toBe(0);
                    wrapper.update();
                    _loop_1 = function (i) {
                        wrapper
                            .findWhere(function (x) { return x.key() === i.toString(); })
                            .find(ExpandIcon_1.ExpandIcon)
                            .simulate('click');
                    };
                    // Expand all nodes
                    for (i = 0; i < 10; i++) {
                        _loop_1(i);
                    }
                    wrapper.update();
                    expect(wrapper.find(TreeNode_1.TreeNode).find(Spinner_1.Spinner).length).toBe(10);
                    wrapper.unmount();
                    return [2 /*return*/];
            }
        });
    }); });
    it('shows an "Error" sub-item if an error occurs while dynamically expanding an item', function () { return tslib_1.__awaiter(void 0, void 0, void 0, function () {
        var wrapper, firstNode, errorSubItem;
        return tslib_1.__generator(this, function (_a) {
            switch (_a.label) {
                case 0:
                    wrapper = enzyme_1.mount(React.createElement(AsyncTree_1.AsyncTree, { onResolveItems: onResolveItemsWithFailures(), strings: defaultStrings }));
                    // Wait for initial list of items to load.
                    return [4 /*yield*/, wait(waitTime + waitTime / 2)];
                case 1:
                    // Wait for initial list of items to load.
                    _a.sent();
                    wrapper.update();
                    firstNode = wrapper.find(TreeNode_1.TreeNode).at(0);
                    // Expand the first node
                    firstNode.find(ExpandIcon_1.ExpandIcon).simulate('click');
                    // Wait for list of items to load.
                    return [4 /*yield*/, wait(waitTime + waitTime / 2)];
                case 2:
                    // Wait for list of items to load.
                    _a.sent();
                    wrapper.update();
                    expect(wrapper.find(TreeNode_1.TreeNode).length).toBe(11);
                    errorSubItem = wrapper.find(TreeNode_1.TreeNode).at(1);
                    expect(errorSubItem.text()).toBe('Error loading. Try again');
                    expect(errorSubItem.find(Icon_1.IconBase).length).toBe(1);
                    expect(errorSubItem.find(Icon_1.IconBase).at(0).props().iconName).toBe('ErrorBadge');
                    expect(errorSubItem.find(Link_1.Link).at(0).text()).toBe('Try again');
                    wrapper.unmount();
                    return [2 /*return*/];
            }
        });
    }); });
    it('allows the user to retry loading items if an error occurs while dynamically expanding an item', function () { return tslib_1.__awaiter(void 0, void 0, void 0, function () {
        var wrapper, firstNode, errorSubItem, retryLink, loadingSubItem;
        return tslib_1.__generator(this, function (_a) {
            switch (_a.label) {
                case 0:
                    wrapper = enzyme_1.mount(React.createElement(AsyncTree_1.AsyncTree, { onResolveItems: onResolveItemsWithFailures(), strings: defaultStrings }));
                    // Wait for initial list of items to load.
                    return [4 /*yield*/, wait(waitTime + waitTime / 2)];
                case 1:
                    // Wait for initial list of items to load.
                    _a.sent();
                    wrapper.update();
                    firstNode = wrapper.find(TreeNode_1.TreeNode).at(0);
                    // Expand the first node
                    firstNode.find(ExpandIcon_1.ExpandIcon).simulate('click');
                    // Wait for list of items to load.
                    return [4 /*yield*/, wait(waitTime + waitTime / 2)];
                case 2:
                    // Wait for list of items to load.
                    _a.sent();
                    wrapper.update();
                    expect(wrapper.find(TreeNode_1.TreeNode).length).toBe(11);
                    errorSubItem = wrapper.find(TreeNode_1.TreeNode).at(1);
                    expect(errorSubItem.find(Link_1.Link).length).toBe(1);
                    retryLink = errorSubItem.find(Link_1.Link).at(0);
                    retryLink.simulate('click');
                    // The "Error" sub-item is no longer present.
                    errorSubItem = wrapper.find(TreeNode_1.TreeNode).at(1);
                    expect(errorSubItem.text()).not.toBe('Error loading. Try again');
                    loadingSubItem = wrapper.find(TreeNode_1.TreeNode).at(1);
                    expect(loadingSubItem.find(Spinner_1.Spinner).length).toBe(1);
                    expect(loadingSubItem.text()).toBe('Loading...');
                    // Wait for list of items to load.
                    return [4 /*yield*/, wait(waitTime + waitTime / 2)];
                case 3:
                    // Wait for list of items to load.
                    _a.sent();
                    wrapper.update();
                    expect(wrapper.find(TreeNode_1.TreeNode).length).toBe(20);
                    // Loading sub-item is no longer present.
                    loadingSubItem = wrapper.find(TreeNode_1.TreeNode).at(1);
                    expect(loadingSubItem.text()).not.toBe('Loading...');
                    expect(wrapper.find(TreeNode_1.TreeNode).find(Spinner_1.Spinner).length).toBe(0);
                    wrapper.unmount();
                    return [2 /*return*/];
            }
        });
    }); });
});
//# sourceMappingURL=AsyncTree.test.js.map