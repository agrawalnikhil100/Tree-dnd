/*!
 * Copyright (C) Microsoft Corporation. All rights reserved.
 */
import { __assign } from "tslib";
import 'jest';
import * as Enzyme from 'enzyme';
import toJson from 'enzyme-to-json';
import * as React from 'react';
import { IconButton } from '@fluentui/react/lib/Button';
import { PaneBase } from './Pane.base';
describe('Pane', function () {
    var wrapper;
    var wrapperProps;
    beforeAll(function () {
        wrapperProps = {
            isOpen: true
        };
    });
    it('renders the pane component', function () {
        wrapper = Enzyme.shallow(React.createElement(PaneBase, __assign({}, wrapperProps)));
        expect(toJson(wrapper)).toMatchSnapshot();
    });
    it('should have the close button', function () {
        wrapperProps = __assign(__assign({}, wrapperProps), { hasCloseButton: true });
        wrapper = Enzyme.shallow(React.createElement(PaneBase, __assign({}, wrapperProps)));
        expect(wrapper.find(IconButton)).toHaveLength(1);
    });
});
//# sourceMappingURL=Pane.base.test.js.map