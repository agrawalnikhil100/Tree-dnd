/*!
 * Copyright (C) Microsoft Corporation. All rights reserved.
 */
import * as Validators from './Validators';
describe('Simple form validators test', function () {
    var customMessage = 'Custom Error Message';
    it('Integer validator', function () {
        expect(Validators.isInteger(customMessage)(null)).toBeFalsy();
        expect(Validators.isInteger(customMessage)(undefined)).toBeFalsy();
        expect(Validators.isInteger(customMessage)('4.1')).toEqual(customMessage);
        expect(Validators.isInteger(customMessage)('4')).toBeFalsy();
        expect(Validators.isInteger(customMessage)('4.0')).toBeFalsy();
    });
    it('Length validator', function () {
        expect(Validators.length(4, function () { return customMessage; })('----')).toBeFalsy();
        expect(Validators.length(4, function () { return customMessage; })('-----')).toBeTruthy();
        expect(Validators.length(4, function () { return customMessage; })(null)).toEqual(customMessage);
        expect(Validators.length(4, function () { return customMessage; })(undefined)).toEqual(customMessage);
    });
    it('MinLength validator', function () {
        expect(Validators.minLength(4, function () { return customMessage; })('----')).toBeFalsy();
        expect(Validators.minLength(4, function () { return customMessage; })('-----')).toBeFalsy();
        expect(Validators.minLength(4, function () { return customMessage; })('---')).toEqual(customMessage);
        expect(Validators.minLength(4, function () { return customMessage; })(null)).toEqual(customMessage);
        expect(Validators.minLength(4, function () { return customMessage; })(undefined)).toEqual(customMessage);
    });
    it('MaxLength validator', function () {
        expect(Validators.maxLength(4, function () { return customMessage; })(null)).toBeFalsy();
        expect(Validators.maxLength(4, function () { return customMessage; })(undefined)).toBeFalsy();
        expect(Validators.maxLength(4, function () { return customMessage; })('----')).toBeFalsy();
        expect(Validators.maxLength(4, function () { return customMessage; })('-----')).toEqual(customMessage);
    });
    it('MinValue validator', function () {
        expect(Validators.minValue(4, function () { return customMessage; })(null)).toBeFalsy();
        expect(Validators.minValue(4, function () { return customMessage; })(undefined)).toBeFalsy();
        expect(Validators.minValue(4, function () { return customMessage; })('----')).toBeFalsy();
        expect(Validators.minValue(4, function () { return customMessage; })('3')).toEqual(customMessage);
        expect(Validators.minValue(4, function () { return customMessage; })('5')).toBeFalsy();
        expect(Validators.minValue(0, function () { return customMessage; })('.8')).toBeFalsy();
    });
    it('MaxValue validator', function () {
        expect(Validators.maxValue(4, function () { return customMessage; })(null)).toBeFalsy();
        expect(Validators.maxValue(4, function () { return customMessage; })(undefined)).toBeFalsy();
        expect(Validators.maxValue(4, function () { return customMessage; })('----')).toBeFalsy();
        expect(Validators.maxValue(4, function () { return customMessage; })('5')).toEqual(customMessage);
        expect(Validators.maxValue(4, function () { return customMessage; })('3')).toBeFalsy();
        expect(Validators.maxValue(4, function () { return customMessage; })('.8')).toBeFalsy();
    });
    it('Regex validator', function () {
        expect(Validators.regex(/^(.)*$/, customMessage)('anything')).toBeFalsy();
        expect(Validators.regex(/^([0-9])*$/, customMessage)('10202')).toBeFalsy();
        expect(Validators.regex(/^([0-9])*$/, customMessage)('letters')).toEqual(customMessage);
    });
    it('Number validator', function () {
        expect(Validators.isNumber(customMessage)('abcd')).toEqual(customMessage);
        expect(Validators.isNumber(customMessage)(null)).toBeFalsy();
        expect(Validators.isNumber(customMessage)('')).toBeFalsy();
        expect(Validators.isNumber(customMessage)('10')).toBeFalsy();
        expect(Validators.isNumber(customMessage)('10.1')).toBeFalsy();
    });
    it('URL validator', function () {
        expect(Validators.isURL(customMessage)('abcd')).toEqual(customMessage);
        expect(Validators.isURL(customMessage)(null)).toBeFalsy();
        expect(Validators.isURL(customMessage)('')).toBeFalsy();
        expect(Validators.isURL(customMessage)('https://www.microsoft.com')).toBeFalsy();
        expect(Validators.isURL(customMessage)('ftp://microsoft.com')).toBeFalsy();
    });
    it('Required validator', function () {
        expect(Validators.required(customMessage)(null)).toEqual(customMessage);
        expect(Validators.required(customMessage)(undefined)).toEqual(customMessage);
        expect(Validators.required(customMessage)('')).toEqual(customMessage);
        expect(Validators.required(customMessage)([])).toEqual(customMessage);
        expect(Validators.required(customMessage)(0)).toBeFalsy();
        expect(Validators.required(customMessage)('HelloWorld')).toBeFalsy();
        expect(Validators.required(customMessage)([1, 2, 3])).toBeFalsy();
        expect(Validators.required(customMessage)(['a', 'b', 'c'])).toBeFalsy();
    });
});
//# sourceMappingURL=Validators.test.js.map