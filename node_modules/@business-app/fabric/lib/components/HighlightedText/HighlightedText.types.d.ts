/*!
 * Copyright (C) Microsoft Corporation. All rights reserved.
 */
import { IStyle, ITheme } from '@fluentui/style-utilities';
import { IStyleFunctionOrObject } from '@fluentui/react/lib/Utilities';
export interface IHighlightedTextProps {
    /** Text to display. */
    readonly text: string;
    /** List of portions of text to highlight. Highlights can overlap, although this is not encouraged. */
    readonly highlights: ReadonlyArray<{
        readonly startIndex: number;
        readonly length: number;
    }>;
    /** Screen-reader labels. */
    readonly labels: {
        /** Screen-reader labels indicating the start of some highlighted text. */
        readonly beginHighlightedText: string;
        /** Screen-reader labels indicating the end of some highlighted text. */
        readonly endHighlightedText: string;
    };
    theme?: ITheme;
    styles?: IStyleFunctionOrObject<IHighlightedTextStyleProps, IHighlightedTextStyles>;
}
export interface IHighlightedTextStyleProps {
    theme: ITheme;
}
export interface IHighlightedTextStyles {
    highlights: IStyle;
}
